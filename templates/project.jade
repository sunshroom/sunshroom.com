extends layout

block prepend title
  | #{ page.title + " - "}

block header
  h1= "Projects: " + page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) Projects
